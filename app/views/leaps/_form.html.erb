<%= form_for(@leap) do |f| %>
  <% if @leap.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@leap.errors.count, "error") %> prohibited this leap from being saved:</h2>

      <ul>
      <% @leap.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :distance %><br />
    <%= f.number_field :distance %>
  </div>
  <div class="field">
    <%= f.label :leap_type %><br />
    <%= f.text_field :leap_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
